<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Simon's Cafe</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,500;0,600;1,500&family=Work+Sans:wght@400;500;600&display=swap" rel="stylesheet">
<style>
  :root {
    --ink: #241d24;
    --ink-deep: #19141a;
    --ivory: #f3ece2;
    --ivory-dim: #cabfc4;
    --plum: #9a5b7a;
    --sage: #7c8a6a;
  }

  * { box-sizing: border-box; }
  html { scroll-behavior: smooth; }

  body {
    margin: 0;
    background: var(--ink-deep);
    color: var(--ivory);
    font-family: 'Work Sans', sans-serif;
    line-height: 1.6;
  }

  h1, h2, h3 { font-family: 'Cormorant Garamond', serif; font-weight: 600; margin: 0; }

  .wrap { max-width: 880px; margin: 0 auto; padding: 0 32px; }

  nav {
    position: sticky; top: 0; z-index: 10;
    background: rgba(25,20,26,0.9);
    backdrop-filter: blur(6px);
    border-bottom: 1px solid rgba(243,236,226,0.08);
  }
  .nav-inner {
    max-width: 880px; margin: 0 auto; padding: 18px 32px;
    display: flex; justify-content: space-between; align-items: center;
  }
  .nav-inner .brand { font-family: 'Cormorant Garamond', serif; font-size: 20px; letter-spacing: 0.02em; }
  .nav-inner .links { display: flex; gap: 26px; }
  .nav-inner .links a { color: var(--ivory-dim); text-decoration: none; font-size: 14px; }
  .nav-inner .links a:hover { color: var(--plum); }

  /* Hero */
  .hero {
    min-height: 78vh;
    display: flex; align-items: center;
    background: radial-gradient(ellipse at 15% 15%, rgba(154,91,122,0.16), transparent 55%),
                radial-gradient(ellipse at 85% 85%, rgba(124,138,106,0.12), transparent 55%),
                var(--ink-deep);
  }
  .hero-inner { padding: 100px 0 70px; }
  .kicker {
    font-size: 13px; letter-spacing: 0.04em; color: var(--plum);
    margin-bottom: 22px;
  }
  .hero h1 {
    font-size: clamp(48px, 8vw, 88px);
    line-height: 0.98;
    color: var(--ivory);
  }
  .hero h1 em { font-style: italic; color: var(--plum); }
  .hero p.lede {
    max-width: 46ch; font-size: 18px; color: var(--ivory-dim); margin-top: 22px;
  }
  .hero-cta { margin-top: 34px; display: flex; gap: 16px; flex-wrap: wrap; }
  .btn {
    display: inline-block; padding: 12px 26px; font-size: 14px;
    border: 1px solid var(--plum); color: var(--ivory); text-decoration: none;
    border-radius: 2px; transition: background 0.2s ease, color 0.2s ease;
  }
  .btn:hover { background: var(--plum); color: var(--ink-deep); }
  .btn.ghost { border-color: rgba(243,236,226,0.3); }
  .btn.ghost:hover { background: rgba(243,236,226,0.08); color: var(--ivory); }

  /* Story */
  .story { padding: 90px 0; border-top: 1px solid rgba(243,236,226,0.08); }
  .story .wrap { display: grid; grid-template-columns: 1fr 1.3fr; gap: 60px; }
  .story h2 { font-size: clamp(26px, 4vw, 34px); margin-bottom: 18px; }
  .story p { color: var(--ivory-dim); }
  @media (max-width: 760px) { .story .wrap { grid-template-columns: 1fr; } }

  /* Menu */
  .menu { padding: 90px 0; background: var(--ink); }
  .menu h2 { font-size: clamp(26px, 4vw, 34px); margin-bottom: 8px; }
  .menu .sub { color: var(--ivory-dim); margin-bottom: 50px; }
  .menu-cols { display: grid; grid-template-columns: 1fr 1fr; gap: 50px; }
  @media (max-width: 700px) { .menu-cols { grid-template-columns: 1fr; } }
  .menu-group h3 {
    font-size: 20px; color: var(--plum); margin-bottom: 20px;
    font-style: italic; font-weight: 500;
  }
  .item {
    display: flex; align-items: baseline; gap: 10px;
    margin-bottom: 16px;
  }
  .item .name { font-size: 16px; color: var(--ivory); white-space: nowrap; }
  .item .leader { flex: 1; border-bottom: 1px dotted rgba(243,236,226,0.25); margin-bottom: 5px; }
  .item .price { font-size: 15px; color: var(--ivory-dim); white-space: nowrap; }

  /* Visit */
  .visit { padding: 90px 0; }
  .visit .wrap { display: grid; grid-template-columns: 1fr 1fr; gap: 60px; }
  .visit h2 { font-size: clamp(26px, 4vw, 34px); margin-bottom: 20px; }
  .hours-row { display: flex; justify-content: space-between; padding: 10px 0; border-bottom: 1px solid rgba(243,236,226,0.08); font-size: 15px; }
  .hours-row .day { color: var(--ivory); }
  .hours-row .time { color: var(--ivory-dim); }
  .addr { color: var(--ivory-dim); margin-top: 4px; }
  @media (max-width: 760px) { .visit .wrap { grid-template-columns: 1fr; } }

  footer {
    padding: 40px 0; text-align: center; color: rgba(243,236,226,0.35); font-size: 13px;
    border-top: 1px solid rgba(243,236,226,0.08);
  }
</style>
</head>
<body>

<nav>
  <div class="nav-inner">
    <div class="brand">Simon's Cafe</div>
    <div class="links">
      <a href="#story">Story</a>
      <a href="#menu">Menu</a>
      <a href="#visit">Visit</a>
    </div>
  </div>
</nav>

<section class="hero">
  <div class="wrap hero-inner">
    <div class="kicker">NEIGHBORHOOD CAFE</div>
    <h1>Simon's <em>Cafe</em></h1>
    <p class="lede">Good coffee, simple food, and a place worth slowing down for. Come as you are.</p>
    <div class="hero-cta">
      <a class="btn" href="#menu">See the menu</a>
      <a class="btn ghost" href="#visit">Find us</a>
    </div>
  </div>
</section>

<section class="story" id="story">
  <div class="wrap">
    <h2>Our story</h2>
    <p>Simon's Cafe started as a small corner spot with a simple goal: pour a great cup of coffee and treat every person who walks in like a regular. We keep the menu focused, the ingredients honest, and the doors open for long mornings and longer conversations.</p>
  </div>
</section>

<section class="menu" id="menu">
  <div class="wrap">
    <h2>Menu</h2>
    <p class="sub">A few of our regulars.</p>
    <div class="menu-cols">
      <div class="menu-group">
        <h3>Coffee & Tea</h3>
        <div class="item"><span class="name">House Espresso</span><span class="leader"></span><span class="price">$4</span></div>
        <div class="item"><span class="name">Flat White</span><span class="leader"></span><span class="price">$5</span></div>
        <div class="item"><span class="name">Vanilla Oat Latte</span><span class="leader"></span><span class="price">$6</span></div>
        <div class="item"><span class="name">Cold Brew</span><span class="leader"></span><span class="price">$5</span></div>
        <div class="item"><span class="name">Chamomile Tea</span><span class="leader"></span><span class="price">$4</span></div>
      </div>
      <div class="menu-group">
        <h3>To Eat</h3>
        <div class="item"><span class="name">Avocado Toast</span><span class="leader"></span><span class="price">$8</span></div>
        <div class="item"><span class="name">Croissant</span><span class="leader"></span><span class="price">$4</span></div>
        <div class="item"><span class="name">Breakfast Sandwich</span><span class="leader"></span><span class="price">$9</span></div>
        <div class="item"><span class="name">Granola & Yogurt</span><span class="leader"></span><span class="price">$7</span></div>
        <div class="item"><span class="name">Daily Pastry</span><span class="leader"></span><span class="price">$4</span></div>
      </div>
    </div>
  </div>
</section>

<section class="visit" id="visit">
  <div class="wrap">
    <div>
      <h2>Hours</h2>
      <div class="hours-row"><span class="day">Mon – Fri</span><span class="time">7:00am – 4:00pm</span></div>
      <div class="hours-row"><span class="day">Sat – Sun</span><span class="time">8:00am – 5:00pm</span></div>
    </div>
    <div>
      <h2>Visit</h2>
      <p class="addr">Your city here</p>
      <p class="addr">hello@simonscafe.com</p>
      <p class="addr">@simonscafe</p>
    </div>
  </div>
</section>

<footer>© 2026 Simon's Cafe. Made with care.</footer>

</body>
</html>
